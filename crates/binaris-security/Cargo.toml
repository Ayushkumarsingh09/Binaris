[package]
name = "binaris-security"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Security vulnerability and secret detection for Binaris"

[dependencies]
binaris-core = { workspace = true }
regex = { workspace = true }
once_cell = { workspace = true }
uuid = { workspace = true }
